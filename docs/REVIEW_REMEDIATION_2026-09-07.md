# 2026-09-07 项目审查修复交接

状态：独立分支实现和离线验收。尚未部署、解除冻结、扩大烟测额度、下单或上传 Loop。

## 分支隔离

| 内容 | 修复分支 | 基线 |
| --- | --- | --- |
| 漏斗、Paper、策略、回测 | `codex/review-fixes-20260907` | `7a6feec` |
| Loop 证据与收益语义 | `codex/loop-review-fixes-20260907` | `990f896` |

两个工作树分别为 `D:/cdoeX-worktrees/ai-quant-review-fixes` 和
`D:/cdoeX-worktrees/ai-quant-loop-review-fixes`。它们基线不同，不应整枝互相覆盖。
生产 `D:/cdoeX-work-harden` 的既有未提交改动保留；本轮没有改调度任务、凭据或线上账本。

## 已实现的核心修复

- A1/A2：预案发布与执行器接管分开记账。有候选但未接管为 `handoff_pending`，不再永久报成功；后续只恢复既有授权的接管，不补造选股结果。飞书保留脱敏错误类型与代码，读取有界重试，写入结果不确定时读回确认。
- A3/A8：单票买不起一股等预期拒绝只阻断该票；传输、账本和订单归属异常仍阻断新仓。唯一 Paper 入口强制校验有限、正数且不超过 100 美元的烟测名义金额；没有通过验收前不能靠漏填变量放开额度。
- A4/A5：已有活动清仓单不撤掉重发；仅在明确终态后，按真实残量创建有次数上限的新退出 ID。部分成交、撤单期间新增成交、重启恢复和未知订单状态均有回归测试。数量矛盾时不能先撤保护单。
- A6/A7：旧入场 intent 只查单、不重放买单；跨日恢复使用明确旧日源和隔离账本，仅处理可证实归属的退出，不产生新买单。旧日有效租约、未知持仓、身份冲突继续阻断。
- 重启恢复：合法保护子单可由已持久化的入场父单证明归属，不再因子单独立返回而误拦。活进程租约过期时不能重复启动；监控恢复持续至实际交易日收盘，15:00 ET 的禁开仓门禁仍有效。
- 通知：成交和故障正文先持久化，网络发送移出持仓管理循环。推送超时不得拖住清仓；不确定是否送达的记录不伪装成功、不自动重复发送。
- B1/B3/B4：当前完整 K 线信号与历史成交回放拆开；真实持仓按实际入场时间判断退出。现代参数、版本和哈希进入预案，授权与代码不符只能继续保护退出，不能开新仓。首入、二次入场共用时间和点差门禁。
- B5/B6：Loop 分离完整冻结观察池、盘后赢家样本、市场反事实表现和实际成交表现。实际损益必须由有来源的成交增量核算；累计订单快照不能直接相加。费用缺失则净损益未知，不能默认零费用。

这些是代码行为修复，不表示它们均曾造成过历史亏损，也不是收益改善证明。

## 策略与资金边界

没有修改市值至少 10 亿美元、相对前收至少上涨 4%、RVOL、只做多、入场点差最多 0.25%、全包止损预算最多 2%、首次/二次风险 60%/40%、15:00 ET 后禁止新仓、15:50 ET 清仓等约束。
止损预算是风险计算约束，不是极端行情中的成交价格保证。

单票账户风险上限仍为 0.5%，它不是名义仓位占比。
烟测名义金额上限仍为 100 美元，因此高于该额度的股票仍可能买不起一整股；本次消除的是它对其他候选的错误全局冻结。

## 历史回测证据

对既有 `current_modern_h15_spread_guard.trades.parquet` 做了只读资格审计：

- 168 次尝试，其中 43 次二次入场。
- 3 次二次入场晚于当前 15:00 ET 截止时间；1 次点差超过当前 0.25% 上限。
- 17 次旧 15:45 时间退出需按当前规则重新回放，不能直接改标签后沿用损益。
- 原文件 SHA-256：`6ed5ab375bf2e62cf7259ccf01bb347e56e8079108b99d09d957883ee8706f62`。

结论为 `invalidated_for_current_strategy`；本轮没有生成“修复后的收益”、新盲测或生产适用证明。
回测入口现要求明确实验元数据，拒绝覆盖旧证据；已看过的数据只能作为探索或诊断样本。

## 尚未完成的验收，不应称为已上线闭环

1. **受控 Paper 发布**：先审核并集成这两个分支，确认运行代码、有效预案哈希和授权一致；保留烟测上限，另行进行真实 Paper 环境中的订单、保护、退出、通知对账验收。离线 mock 通过不等于券商端验收通过。
2. **跨日恢复操作**：仅在确有旧日自有残仓时，由操作者指定旧日来源，使用 `--recover-prior-day-only` 与明确 `--source-trade-date`。恢复与正常交易互斥；不能因恢复成功自动解除冻结。
3. **Loop 生产证据提供器**：本轮有明确本地输入和严格校验，但没有把所有生产计划、完整观察池与券商成交增量自动导出并接到现有定时上传。完整参数来源、成交增量和成本证据缺失时必须保留 unavailable。新的远端字段兼容性尚未联网验收，既有已投递事件不改写。
4. **B2 正期望验证**：按修复后唯一配置重新回测，并在新的时间窗口做样本外/前向验证。增加本金不是策略优化，不能承诺盈利。
5. **B7 机会池设计**：09:35 暂未确认与全天永久失效的区分，仍需原冻结池内的对照实验；本轮没有用收盘赢家作后视镜放宽线上漏斗。

最终离线验证：主运行分支 **911 项测试通过**，Ruff 全库通过，严格 mypy **444 文件通过**；
Loop 分支 **826 项测试通过**，相关 Ruff、严格 mypy **13 文件通过**。
compileall 和差异格式检查通过。两个分支的测试数量不应相加称为独立测试总数。

具体命令、结果和范围见仓库根目录 `PROGRESS.md` 的 M90；Loop 独立分支另见
`operations/loop_integration/REVIEW_FIXES.md`。
