# 2026-09-22 事件模型发布验收

## 当前状态

代码准备提交审查；尚未切换生产调度，未下单，未上传模拟成交或伪造 Loop 样本。
本次只验证工程链路，不代表策略盈利已经验证。

## 本轮修复

- 全仓严格 Mypy 的 54 处问题已修复：补齐测试输入类型，纠正 Polars schema
  声明和模块导入，移除冗余 cast。仅对故意传入错误类型的安全测试保留精确忽略。
- 投资事件记录提供方已显式切换为 `vps-work`。四张多维表分别承载选股、盯盘、
  成交和复盘；本地持久意图先于远端追加，未知写入结果不会盲目重发。
- 发布安装器不再生成替代策略。必须传入原有已批准策略与持久状态目录，并验证
  新发布目录的 `runs` 真实指向该目录，拒绝全新空账本或错误绑定。
- 切换前拒绝仍启用或运行的旧任务，以及残留 Paper/调度/supervisor 进程和
  supervisor 自启动入口。安装的所有任务默认禁用，不自动打开下单开关。
- 独立复审识别出的 supervisor 遗漏已通过红绿回归补齐；复审未再发现该发布
  防护范围内的重要问题。实际账本迁移仍是上线前独立验收步骤。

## 验证证据

- 隔离发布环境完整 pytest：**1337 passed in 91.93s**，退出码 0。
- Ruff、完整 Mypy（522 个源文件）和 Python 编译检查通过。
- Electron 服务测试 33 项、前端状态测试 12 项通过；Vite 生产构建通过。
- 无网络 Paper 安全演练通过：时钟、重复进程、故障升级、强制清仓、意图恢复、
  kill-switch、outbox 幂等、未知状态冻结；券商调用和外部写入均为 0。
- Python 3.12 编译检查、差异空白检查通过。
- 提交前扫描整套改动中的已配置密钥和常见凭据模式，无发现；这不是完整安全审计。

## 实际接口检查

- Alpaca Paper 账户为 ACTIVE；检查时持仓 0，活动订单 0；客户端写入关闭。
- Alpaca SIP 最新 SPY 报价接口返回 HTTP 200。报价时间相对本机时间曾领先约
  1.45 秒：证明数据可取，不代表该报价满足入场时效门槛；部署前应检查本机时钟。
- `vps-work` 身份与四张专用表的 schema、读取和写后回读均通过；同一部署审计事件
  重放返回同一记录 ID，没有生成重复记录。该审计明确不是选股或成交证据。
- Livermore 频道检查通过，已发送一次中文“发布联调测试”，明确不是成交、不是
  上线成功。UTF-8 可逆、无问号/替代字符；消息 ID：
  `c36762ed-67d0-48ff-b429-4b9f5c7db6d4`。
- 正文及发送回执在本地 `runs/acceptance/release-notifications.sqlite3`，与交易
  成交审计分离；重复执行同一通知键不会重复推送。

## 发布运行环境

旧解释器出现 numpy、pandas、websockets、yfinance、tzdata 五组重复发行元数据。
未卸载或覆盖正在使用的环境。新建独立 Python 3.12.6 环境：

`D:\cdoeX-work\runtime\ai-quant\releases\20260922-event-v1`

按仓库两份 requirements 安装 74 个包，`uv pip check` 确认依赖兼容。该目录
尚未绑定生产任务。它与旧环境的测试结果分别记录。

## 正式切换前仍须完成

1. GitHub PR 的 CI 和合并审批；正式发布只用合入 main 的固定提交。
2. 停用并排空旧调度，备份/核对各旧根目录账本和不可变回执；冲突不覆盖、不补造。
3. 依照 `docs/DEPLOYMENT.md` 映射持久状态、保留已批准策略和回滚路径。
4. 再核对 Paper 持仓/订单与本地状态、时钟、新闻与股本覆盖率和完整波次耗时，
   然后启用唯一现代漏斗。20 万美元是总名义上限，不是强制满仓目标。
