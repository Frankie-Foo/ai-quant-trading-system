{
  "name": "adaptive-trading-client",
  "private": true,
  "version": "0.1.0",
  "description": "Read-only evidence-backed U.S. equity research desktop",
  "author": "Trading System v2",
  "type": "module",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "desktop": "npm run build && electron .",
    "desktop:analyst": "npm run build && electron electron/analyst-main.cjs",
    "test:electron": "node --test electron/test/*.test.cjs",
    "icon:mac": "bash scripts/build_macos_icon.sh",
    "dist:win": "npm run build && electron-builder --win nsis",
    "dist:mac:analyst": "npm run build && npm run test:electron && npm run icon:mac && electron-builder --config electron-builder.analyst.yml --mac dmg zip"
  },
  "dependencies": {
    "lucide-react": "^0.468.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.6.0",
    "electron": "^37.2.6",
    "electron-builder": "^26.0.12",
    "vite": "^6.1.0"
  },
  "build": {
    "appId": "cn.frank.adaptive-trading-client",
    "productName": "日内量化决策台",
    "files": [
      "dist/**/*",
      "electron/**/*",
      "package.json"
    ],
    "directories": {
      "output": "release"
    },
    "win": {
      "target": "nsis"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    }
  }
}
